docs/
*.pkg
.*.swp
build*/

# NOTE: unlike mavericks-1password (whose real launcher is the hand-written bin/op,
# so it ignores the generated bin/1password), Signal's launcher IS the generated
# bin/signal-desktop -- it is tracked, not ignored.

# Claude Code scratch dir (CLAUDE_CODE_TMPDIR)
.tmp-claude/

# macOS cruft (baseline parity with the other mavericks-* repos)
._*
.DS_Store

# Full version (<version>-mavericks.N): a build product, stamped at release. UPSTREAM_VERSION is committed.
/VERSION
/dist/
